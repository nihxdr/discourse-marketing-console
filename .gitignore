# Secrets
.env

# Seed run output (contains instance URL + created IDs)
created_content.json

# Python
__pycache__/
*.pyc
.venv/
venv/

# Node / wrangler
node_modules/
package-lock.json
worker/package.json

# OS / editor
.DS_Store
*.swp

# Local-only seeder (contains seed-persona passwords)
discourse-seed.py
